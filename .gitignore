# Root .claude/ is local scratch (e.g. participant-installed skills), except the
# sorting-hat helper which ships with the workshop.
/.claude/*
!/.claude/skills/
/.claude/skills/*
!/.claude/skills/sorting-hat/
.env
logs/
*.duckdb
*.duckdb.wal
exercises/benchmark/results/
skills-lock.json


terraform.tfvars
.terraform

# terraform plan artifacts
tfplan

# local planning/export scratch, not for the repo
context/
