# dbt artifacts, regenerated by `dbt build`
target/
dbt_packages/
logs/
.user.yml

# uv
.venv/

# NOTE: warehouse.duckdb is gitignored (*.duckdb in the root .gitignore) and
# rebuilt at IDE startup by data/build.sh (.vscode/tasks.json). Only sample.csv
# is committed, as the warm-up dataset; its rebuild is deterministic.
