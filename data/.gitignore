# dbt artifacts — regenerated by `dbt build`
target/
dbt_packages/
logs/
.user.yml

# uv
.venv/

# NOTE: warehouse.duckdb and sample.csv ARE committed on purpose —
# they are the workshop's source of truth (the "poetic" pre-built warehouse).
